import { Input, TextArea } from "./input";

export default {
    title: "Input",
    component: Input,
};

export const Default = () => {
    return <Input />;
};

export const InputArea = () => {
    return <TextArea />;
};
